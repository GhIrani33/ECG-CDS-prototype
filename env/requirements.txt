# Python 3.9.x
numpy==1.26.4
pandas==2.2.2
pyarrow==15.0.2
scikit-learn==1.4.2
scipy==1.12.0
tqdm==4.66.4
wfdb==4.1.2
matplotlib==3.8.4

# Torch (Windows, CUDA 11.8 build you used)
torch==2.1.2+cu118
torchaudio==2.1.2+cu118
# torchvision optional if not used
# torchvision==0.16.2+cu118
# NOTE: On Windows use the official wheel index; see README for install cmd.

# Fairseq (stable you used)
fairseq==0.12.2

# fairseq-signals from source (install via README instructions, not pip index)
# left blank here intentionally
